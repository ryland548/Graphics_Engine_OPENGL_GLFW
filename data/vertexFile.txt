#version 410 core
layout (location = 0) in vec3 aPos;
layout (location = 1) in float aColorDarkener;

out float colorDarkener;

uniform mat4 camMatrix;
uniform mat4 model;

void main() {
    gl_Position = camMatrix * model * vec4(aPos, 1.0);
    colorDarkener = aColorDarkener;
}
